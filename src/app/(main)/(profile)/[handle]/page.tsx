type ProfileTimelinePagePropsType = {}

export default async function ProfileTimelinePage({ }: ProfileTimelinePagePropsType) {
    return (
        <div>
            ProfileTimeline
        </div>
    );
}
