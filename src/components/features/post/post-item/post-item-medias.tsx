export type MediaGridProps = {
    mediaList: string[];
}

export default function MediaGrid({ mediaList }: MediaGridProps) {
    return;
    return (
        <div>
            <p>MediaGrid</p>
        </div>
    )
}